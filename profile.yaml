schema_version: 1
candidate:
  explanation_language: zh-TW
  coding_language: C++17
  interview_style: Google
objective:
  primary: Google-style software engineering coding interviews
  optimize_for:
    - problem_solving
    - correctness
    - complexity_analysis
    - testing
    - verbal_communication
  do_not_optimize_for:
    - raw_problem_count
training_policy:
  no_full_solution_before_attempt_minutes: 30
  required_edge_cases: 3
  review_intervals_days: [1, 3, 7, 21]
  daily_target_minutes: 60-90
  weekly_problem_target: 8-12
baseline_context:
  recently_practiced_or_studying:
    - leetcode: 560
      topic: Prefix Sum / Hash Map
    - leetcode: 378
      topic: Binary Search / Matrix
    - topic: Binary Search
curriculum:
  source: 靈茶山艾府 LeetCode 常用資料結構題單
  url: https://leetcode.cn/discuss/post/3583665/fen-xiang-gun-ti-dan-chang-yong-shu-ju-j-bvmv/
  active_track: Common Techniques and Data Structures
  active_module: Phase A - Enumeration, Prefix Sum, and Hash Map
  current_problem:
    leetcode: 974
    title: Subarray Sums Divisible by K
  amazon_track:
    roadmap: AMAZON_INTERVIEW_ROADMAP.md
    started: 2026-09-01
    completed_drills: [819, 937, 49, 128, 200, 994, 238, 347, 394, 735, 3, 53, 56]
    next_problem:
      leetcode: 763
      title: Partition Labels
weaknesses:
  - tends to skip clarification and brute-force reasoning before optimized code
  - needs deliberate off-by-one checks for legal window counts and index ranges
  - reports complexity by feel instead of deriving it (819 O(nB), 49 n log k, 128 n*k; 56 answered O(n) for a sort-based algorithm); near graduation - self-stated amortized argument in 735, transferred to LC 3, and 56 self-corrected to O(n log n) with the because-clause after a single prompt
  - carries per-iteration state across loop iterations and relies on a sibling branch to reset it (128 cnt)
  - variable-name and API-spelling inconsistencies across drafts (isalpha, l_m/lm, original_str/original; grid/g in both 200 and 994; queue pop_front in 994)
  - refactors a passing solution without re-running tests (49 draft 3 regressed)
  - resubmits near-identical drafts after a multi-item bug list instead of addressing each item (improved by 200: fixed four items in one draft)
  - skips direct interviewer questions and submits code instead (200 two skipped; 994 three skipped; 994 redo 09-04 three skipped again — recurring Earn Trust risk)
  - first drafts ignore explicit written requirements from the prompt (994 — mark-on-push and fresh tracking both listed, both skipped)
  - guesses edge-case expected outputs instead of walking the definition (994 all-zero grid, wrong again on 09-04 redo; 238 scored 0/3 — corrected in 347 with 3/3); notation half persists — 09-07 review wrote the empty result as [0] instead of []
  - writes results into a variable nothing reads (994 redo found never set; 394 cur written twice while ss was returned) — named pattern: declaration/assignment/READ three-liner
  - line-pointing fix-list verification submitted once ever (735, 2026-09-05); 56 demanded it four times including a hard gate and it was never submitted; three drills running
  - TOP WEAKNESS - does not apply a fix list that names the exact line and the exact text (994 orangesRottings twice; 735 ans.reverse() after being handed reverse(ans.begin(), ans.end()); 56 interval->intervals skipped with the spelling and 3 line numbers given, and the same push_back [0] error survived 3 explicit corrections); rule - open the file and fix each item before replying, never in bulk from memory
  - variable shadowing recurred until it caused a real infinite loop (735 draft 1; warned in 994 redo and 394)
  - clarify questions answerable from stated constraints (238 empty array, 347 k vs uniques, 394 k=0; first clean drill LC 3 on 09-05 — answered "constraints are clear")
  - container API mixups under time pressure (994 queue pop_front, 347 priority_queue push_back)
  - skips required test walks before submitting (238 skipped twice, LC 3 skipped — draft 1 returned 7 on a 6-char string); first walk line delivered on 53, 2026-09-07, alongside the first draft-1 pass
  - constructs adversarial cases without testing them against the wrong solution (53 [2,3,-9,4] lets the reset-to-zero bug pass); rule — run the wrong algorithm on the case before submitting it
  - leaves the sign-compare warning unfixed (i < v.size()); 5 drills running before 56 fixed it on draft 3, and only after being listed twice
strengths:
  - maintains fixed-size sliding-window sums in O(n) time
  - recognizes integer comparisons that avoid unnecessary floating-point division
  - asks for guidance when stuck instead of going silent (49 edge-case derivation)
  - once given a skeleton, completes all parts correctly in one draft (937 comparator)
  - fixes a full multi-item bug list in a single pass when deliberate (200 fixed 4 items; 994 fixed 7 items — but 238 relapsed to one-item fixes twice)
  - can propose and justify a valid alternative design (994 — grid rescan instead of fresh counter)
  - retains structural patterns across sessions (994 redo — layer snapshot, mark-on-push, bounds check all correct from draft 1 two days later)
  - states correct approach verbally on first try even when code lags (238 both versions)
  - self-caught a typo before resubmitting for the first time (238 stray bracket)
  - asks before coding when a concept is unclear instead of guessing (347 heapify constructor; 394 adversarial-case construction — then built a valid nested case with correct expected output)
  - multi-digit number accumulation (num*10 + digit) written unprompted on first attempt (394)
  - first fully self-stated amortized because-clause (735 pre-code, unprompted after three drills of teaching)
  - admits not understanding a fix item and asks (735 "1,2 no understand"), then passes the concept check
  - self-constructed a valid adversarial case unassisted for the first time (LC 3 "zaaac" with the shrink-by-one reasoning)
  - fastest bug recovery to date - LC 3 draft 2 cleared both logic bugs in one pass, 1/8 -> 8/8
  - first draft-1 pass of the track (53 Maximum Subarray, 10/10, 2026-09-07)
  - accepts a counterexample and states the fix himself (53 — shown all-negative, proposed ans = nums[0] unaided)
  - spaced-repetition recall is strong — 09-07 review 4.5/5 across five problems, including two that were wrong during their drills (238 walk, 347 heap direction)
  - accepts a complexity correction and re-derives it properly in one pass (56 - O(n) -> O(n log n) with the sort-dominates because)
  - finds the structural core of a sweep once pointed at the right variable (56 - switched the overlap test to the running end after one hint)
last_updated: 2026-09-07
