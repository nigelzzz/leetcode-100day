session:
  date: 2026-09-19
  track: embedded (Roku prep sheet, C/C++ rapid-fire)
  type: rapid-fire, 60-90 s spoken answers graded against required points

results:
  Q1_stack_vs_heap:
    first_attempt: 1/5 ("static / dynamic allocation" - stack is automatic, not static)
    retry: PASS 5/5 in his own words; polish - say why (stack pointer vs allocator search) and the consequence (stack overflow, which is also why DFS space is O(m*n))
  Q2_static: PASS 3/3 (file-scope visibility, function keeps value, class shared); added internal linkage, .data/.bss, non-reentrancy with ISRs, static member functions have no this
  Q3_volatile: PASS 3/3 (prevents optimisation, MMIO and ISR, not atomic); added the while(!flag) hoisting example, count++ race, not a memory barrier
  Q4_memcpy_vs_memmove: taught on request (overlap, direction choice, size_t reverse-loop pitfall); verified my_memmove 6/6; user skipped the re-say
  Q5_struct_padding:
    first_attempt: '1/3 - 12 correct by accident via a 4 x 3 model; reorder answered with bit-fields; pack cost unknown'
    misconception: 'believed every member is padded to the largest size; repeated on two follow-up checks (char,int,short answered 8; char,short,char,int laid out as 16)'
    resolved: re-taught with a parking-lot model and five measured micro-structs; then {char, short} answered correctly (a=0, s=2, sizeof 4)
    note: user noticed answers were always 4 or 8 - shown sizes 3, 5, 6, 16 once int is removed

progress: 4 of 12 C/C++ rapid-fire passed; Q4 pending re-say
next: Q6 endianness and ntohl (tie to the WiFi bug)
